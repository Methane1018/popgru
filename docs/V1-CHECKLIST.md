# 通往 1.0 的清單

> 2026-09-14 建立。決策背景見 [V1-PLAN.md](V1-PLAN.md)、[LORE.md](LORE.md)、[RABBITHOLE.md](RABBITHOLE.md)。

## 現在在哪

- ✅ 地基重構完成（v0.12.1）—— 寫入與讀取的決策都在 `js/plan.js`，測得到
- ✅ 世界觀定案（賽博）、夥伴定案（家豪，不自覺的帶原者）
- ✅ 開場劇本寫完、11 句閒聊寫完（`js/story.js`）
- ✅ 六張新姿勢的照片已經在 `src/`（還沒去背）

---

## 你做的

### 美術
- [ ] 家豪立繪 `normal` / `surprised` / `sad`（900×1200 透明 PNG，臉在上方三分之一）
- [ ] 家豪**角落小圖**（約 80px 高，剪影要清楚；他在閒聊裡討椅子，可以先畫坐地上）
- [ ] 電子裂縫（想畫就畫；不畫的話我用 CSS 做，電子感反而合適）
- [ ] 錘子、球棒、液壓機
- [ ] 分身格魯被各種工具攻擊的樣子

### 劇本
- [ ] 閒聊從 11 句補到 20～30 句（範本是 b11：讀起來像爛梗，事後才是線索）

### 只有你能決定
- [ ] **裂縫什麼時候出現**（建議：這一次連續壓到某個下數。理由是它剛好對應「訊號被追蹤到」）
- [ ] **故事回顧的入口放哪**（導覽列多一顆？還是收在「我的格魯」裡？）
- [ ] **分身格魯幾隻、怎麼排**（固定位置，還是隨解鎖數量重排）
- [ ] **`自動液壓機` 保留當「解鎖放置系統」的鑰匙，還是改名成錘子**
      （傾向保留 —— 解鎖一整個系統比解鎖一個工具更配得上一條軸的終點）

---

## 我做的

### 素材
- [ ] 重建去背流程（macOS Vision），把六張照片去背
- [ ] 對齊到共用畫布（前五張可以；第六張是特寫，另外處理）
- [ ] 把新姿勢接進遊戲（里程碑歡呼、朋友來訪打招呼、攤了之後爬起來……）

### 劇情
- [ ] 劇情引擎：對白／選擇／`loop`／`byCount`／`vanish`
- [ ] **兩種對話介面**（開場＝全畫面對話框＋立繪；角落＝小泡泡，**絕對不擋壓格魯**）
- [ ] 裂縫 → 敲一下變大 → 家豪跳出來
- [ ] 角落的家豪、點他會講閒聊
- [ ] 故事回顧面板（看完解鎖「導入」、可重跑、重跑不改任何進度）
- [ ] 閒聊收集檢視（沒有任何效果）
- [ ] 存檔欄位：`storySeen`、`storyAt`、`bubblesSeen`（照下面的固定流程走）
- [ ] `bump.sh` 要加 `story.js`（跟當初漏掉 `plan.js` 是同一個坑）

### 系統
- [ ] **里程碑往上加** ⚠️ 放置上線前必做 —— 現在最高一百萬，放置會很快破掉，
      破掉之後 `MILESTONES.find(m => m.at > shown)` 找不到東西，進度條會直接消失
- [ ] 技能樹改成單一起點 ＋ 跨支節點（要兩條支線都有進度才點得起）
- [ ] 放置手段：錘子／球棒／液壓機，掛在新的樹上
- [ ] 分身格魯的畫面（主格魯不被自動化碰到）
- [ ] 整體平衡

---

## 誰卡住誰

```
家豪立繪 ──────────→ 劇情才能真的上線
                     （引擎可以先做，用佔位圖擋著）

技能樹重構 ────────→ 放置手段（工具要掛在樹上，先重構才不用做兩次）

里程碑往上加 ──────→ 放置手段（不然進度條會消失）

工具與分身的美術 ──→ 放置手段的畫面
```

---

## 建議的版本切分

每一版都要能單獨上線（這是專案原則）。

| 版本 | 內容 | 為什麼排這裡 |
|---|---|---|
| **v0.13** | 六張新姿勢去背、接進遊戲 | 素材已經有了，最快看得到東西 |
| **v0.14** | 劇情引擎 ＋ 家豪登場 ＋ 回顧 ＋ 閒聊收集 | 你最想做的部分，而且完全獨立 |
| **v0.15** | 技能樹長大 ＋ 里程碑往上加 | 放置的地基 |
| **v0.16** | 放置手段 ＋ 分身格魯 | 需要上面兩個先到位 |
| **v1.0** | 整合、平衡、收尾 | |

---

## 固定流程：新增一個「會存起來」的欄位

這是二十三條開發紀錄濃縮出來的。**每次加欄位都照這張走**，
v0.11 那一串災難全部是漏掉其中一兩步。

1. **`blankMe()` 給預設值** —— 少了這步，舊裝置的鏡像會缺鍵，
   `undefined` 流進去會讓 Firestore **拒絕整批寫入**（v0.10.1）
2. **決定它是哪一種**，然後照那一種的規則掛：
   | 種類 | 怎麼寫 | 怎麼讀 |
   |---|---|---|
   | 絕對值（只有這台裝置在寫） | 加進 `ABSOLUTE_FIELDS` | `srvAbsolutes` 要有對應 |
   | 只增不減的清單 | `queueUnion` 或整份 union | `planSnapshot` 用 `mergeOwned` |
   | 計數表 | 巢狀物件（**不能用點號路徑**，v0.11.4） | `mergeCounts` |
   | 加減的數字 | `queueInc` | 快照要加回 `inc('欄位')`（v0.10.9） |
3. **訪客模式也要存**（`saveGuest` / `applyGuest`）
4. **任何改動它的地方都要同時改本機** —— `flush()` 會拿本機值寫出去，
   只改伺服器的話幾秒後就被自己蓋掉（v0.11.3）
5. **加測試**。`plan.js` 是純的，所以測得到 —— 把 bug 種回去確認測試會紅
6. **跑 `node check.mjs`**，它會抓上面大部分的疏漏
7. 新檔案有 `?v=` 的話，**`bump.sh` 要加進去**（不然瀏覽器永遠拿舊版）

Firestore 規則不用改 —— `users/{uid}` 是 `allow write: if isMe(uid)`，
新欄位本來就寫得進去。（但如果新欄位會被**別人**寫，那就要加規則。）

---

## 每次出貨前

```bash
./test/run.sh     # 純邏輯測試
node check.mjs    # 靜態檢查
./bump.sh         # 同步版本號（會順便跑 check）
```

`bump.sh` 會在檢查沒過時擋下來。**不要繞過它。**
